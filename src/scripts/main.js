// The one carousel on the page runs the peacock effect, so that is the only
// effect worth shipping. Both files are copied unmodified from the tarot and
// tarot-effects repos.
import './tarot.esm.js';
import './effects/peacock.esm.js';
